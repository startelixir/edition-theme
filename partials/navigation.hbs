{{#unless isSecondary}}
    {{#foreach navigation}}
        <a class="menu-item menu-item-{{slug}}{{#if current}} menu-item-current{{/if}}" href="{{url absolute="true"}}">{{label}}</a>
    {{/foreach}}
{{else}}
    {{#foreach navigation}}
        <a class="menu-item menu-item-{{slug}}{{#if current}} menu-item-current{{/if}}" href="{{url absolute="true"}}">{{label}}</a>
        {{!-- Omit the separator after the last menu entry --}}
        {{#unless $last}}<span class="sep">•</span>{{/unless}}
    {{/foreach}}
{{/unless}}